/*
================================================
نام فایل:
AddCustomerUseCase.kt

ماژول:
Customer Management

وظیفه:
مدیریت عملیات اضافه کردن مشتری جدید.

قانون معماری:
ثبت مشتری از UI مستقیم انجام نمی‌شود.
درخواست ابتدا وارد UseCase شده و سپس به Repository منتقل می‌شود.

================================================
*/

package com.wstoremgdata.customer.usecase

/**
 * منطق ثبت مشتری جدید در این کلاس قرار می‌گیرد.
 */
class AddCustomerUseCase {

    fun execute() {
        // اتصال به Repository در مرحله تکمیل Customer Module انجام می‌شود.
    }
}

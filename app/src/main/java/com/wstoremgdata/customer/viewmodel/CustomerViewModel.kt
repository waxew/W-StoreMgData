/*
================================================
نام فایل:
CustomerViewModel.kt

ماژول:
Customer Management

وظیفه:
مدیریت وضعیت داده‌های مشتری و ارتباط بین UI و UseCase.

توضیح:
این فایل نباید مستقیماً با Database کار کند.
تمام عملیات از طریق لایه UseCase انجام می‌شود.

ارتباط:
UI -> ViewModel -> UseCase -> Repository

================================================
*/

package com.wstoremgdata.customer.viewmodel

/**
 * ViewModel پایه ماژول مشتری.
 *
 * در مراحل بعدی:
 * - StateFlow
 * - Loading State
 * - Error Handling
 * - Pagination
 * به این بخش اضافه خواهد شد.
 */
class CustomerViewModel {

    // وضعیت اولیه ماژول مشتری
    // منطق اصلی در UseCase قرار می‌گیرد.

}

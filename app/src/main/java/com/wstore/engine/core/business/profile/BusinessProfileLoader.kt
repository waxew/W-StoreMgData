package com.wstore.engine.core.business.profile

/**
 * مسئول بارگذاری Business Profile در زمان اجرای برنامه.
 *
 * در نسخه های بعدی این کلاس می تواند فایل JSON یا منبع خارجی را بخواند.
 */
class BusinessProfileLoader {

    fun load(profile: BusinessProfile): BusinessProfile {
        return profile
    }
}

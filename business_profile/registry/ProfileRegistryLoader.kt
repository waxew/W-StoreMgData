package business_profile.registry

/**
 * مسئولیت:
 * بارگذاری Profile های کسب و کار و آماده سازی آن‌ها برای Runtime.
 *
 * هدف:
 * اضافه کردن پروفایل جدید بدون تغییر Core برنامه.
 */
class ProfileRegistryLoader {

    fun loadProfiles(): List<String> {
        // قرارداد اولیه Loader.
        // خواندن فایل‌های JSON پروفایل در مرحله بعد متصل می‌شود.
        return emptyList()
    }
}

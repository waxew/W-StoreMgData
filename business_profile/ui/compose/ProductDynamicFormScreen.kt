package business_profile.ui.compose

/**
 * صفحه فرم محصول پویا
 *
 * این فایل نقطه اتصال Product Create/Edit با Dynamic Form Renderer است.
 * UI بر اساس Business Profile و Schema ساخته می‌شود.
 */

class ProductDynamicFormScreen {
    fun render() {
        // Compose implementation is connected in UI layer.
    }
}
